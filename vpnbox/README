## Deploy vpnbox to digital ocean manually

create keys in conf from README.ssl

all droplets:
hostname vpnbox
size smallest
region San Francisco 1
ssh key (personal key)

create droplet from snapshot centos6_32_baseinstall_prod
add server and ca keys to conf from README.ssl

DO_IP=<ip>
scp -o StrictHostKeyChecking=no -r . root@$DO_IP:/vagrant
ssh -o StrictHostKeyChecking=no root@$DO_IP /vagrant/src/vpnbox.sh
ssh -p42422 -o StrictHostKeyChecking=no -t -i conf/id_rsa futel@$DO_IP "sudo rm -rf /tmp/vagrant"
ssh -p42422 -o StrictHostKeyChecking=no -t -i conf/id_rsa futel@$DO_IP "sudo halt now"
save snapshot vpnbox

create droplet from snapshot vpnbox

## set up openvpn client
# XXX this is really done on the asterisk box
add client keys and client.conf from README.ssl
# XXX make this a service
# cd conf && sudo openvpn --config client.conf
