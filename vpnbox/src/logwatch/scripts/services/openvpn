while (defined($ThisLine = <STDIN>)) {
    # Report any unmatched entries...
    push @OtherList,$ThisLine;
}

if ($#OtherList >= 0) {
   print "\n**Unmatched Entries**\n";
   print @OtherList;
}

exit(0);
